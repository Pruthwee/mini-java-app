# Cloud-ready application configuration
# Environment-specific values are sourced from Azure App Configuration, Azure Key Vault,
# and environment variables at runtime. This file intentionally contains no immutable
# environment-specific properties.
