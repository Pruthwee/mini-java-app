# Configuration has been externalized to Azure App Configuration and environment variables.
# This file is retained only as a minimal placeholder for local bootstrap compatibility.
app.config.source=azure-app-configuration
azure.app.configuration.connection-string=${AZURE_APP_CONFIGURATION_CONNECTION_STRING:}
azure.keyvault.url=${AZURE_KEY_VAULT_URL:}
azure.storage.connection-string=${AZURE_STORAGE_CONNECTION_STRING:}
azure.storage.blob-endpoint=${AZURE_STORAGE_BLOB_ENDPOINT:}
azure.storage.container=${AZURE_STORAGE_CONTAINER:mini-app-data}
server.port=${SERVER_PORT:8080}
