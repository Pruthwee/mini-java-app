apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mbasic-ingress
  namespace: mbasic
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing
spec:
  rules:
    - host: mbasic.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mbasic-service
                port:
                  number: 80
